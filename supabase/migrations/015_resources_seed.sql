-- Seed note: run POST /api/admin/resources/seed from admin after migration 013
-- Or rely on in-app defaults fallback until seeded.

-- Optional: ensure unique constraint name for upsert on resource_pages.slug (already UNIQUE)

SELECT 1;
